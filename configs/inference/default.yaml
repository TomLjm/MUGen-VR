# 推理配置
device: cuda:0
dtype: float16

retrieval:
  top_k: 5
  similarity_weights:
    text: 0.4
    image: 0.3
    audio: 0.3
  rerank: true

generation:
  num_frames: 16
  fps: 8
  guidance_scale: 3.0
  num_inference_steps: 50
  seed: 42

output:
  save_dir: ./outputs
  save_video: true
  save_frames: false
